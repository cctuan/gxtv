'use strict';

define([
	'jquery',
	'backbone',
	'react'
], function($, Backbone, React) {
	var ChannelListComponent = React.createClass({
		render: function() {
			return (
				<div>ChannelListComponent</div>
			);
		}
	});
	return ChannelListComponent;
});
