'use strict';

define([
	'jquery',
	'backbone',
	'react'
], function($, Backbone, React) {
	var SearchComponent = React.createClass({
		render: function() {
			return (
				<div>Search</div>
			);
		}
	});
	return SearchComponent;
});
